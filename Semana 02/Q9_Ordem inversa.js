const prompt = require('prompt-sync')();

//ENTRADA
const A = Number(prompt('1º número: '))
const B = Number(prompt('2º número: '))

//SAÍDA
console.log(`A ordem inversa dos valores digitados é ${B} e ${A}`)